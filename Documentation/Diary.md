1. Setting up my first project.
    -Express engine : has to run before git init
        -This is done so we may have a project and folders to add the .git folder.
    -npm install    : remember to add our packages-lock.json & packages.json files
    -jest           : need a dummy.test.js for not show errors in CI linter

2. 